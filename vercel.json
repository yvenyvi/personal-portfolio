{
  "buildCommand": "mkdir -p dist && cp index.html dist/ && cp -r assets dist/ 2>/dev/null || true && cp -r css dist/ 2>/dev/null || true && cp -r js dist/ 2>/dev/null || true",
  "outputDirectory": "dist"
}
